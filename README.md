# Konjofoods Website

A stunning, futuristic, and professional static website for **Konjofoods**, showcasing premium Ethiopian spices with modern design and smooth interactions.

## 🚀 Features

-   **Multi-page navigation:** Home, About, Products, and Contact pages.
-   **Interactive Product Filtering:** Search and filter products by category.
-   **Modern UI/UX:** Smooth animations, transitions, and a responsive design for all devices.
-   **Contact Form:** A functional contact form that saves messages to local storage.

## 🛠️ Tech Stack

### Frontend

-   **React:** A JavaScript library for building user interfaces.
-   **React Router:** For declarative routing in a React app.
-   **Tailwind CSS:** A utility-first CSS framework for rapid UI development.
-   **Shadcn/UI:** A collection of re-usable UI components.
-   **Lucide React:** A library of beautiful and consistent icons.
-   **Sonner:** A toast notification library for React.

### Backend

-   **Python:** A high-level, general-purpose programming language.
-   **Flask:** A lightweight WSGI web application framework.

## 📦 Getting Started

### Prerequisites

-   Node.js and npm (for the frontend)
-   Python and pip (for the backend)

### Installation & Setup

1.  **Clone the repository:**
    ```bash
    git clone https://github.com/your-username/konjofoods-website.git
    cd konjofoods-website
    ```

2.  **Frontend Setup:**
    ```bash
    cd frontend
    npm install
    ```

3.  **Backend Setup:**
    ```bash
    cd ../backend
    pip install -r requirements.txt
    ```

### Running the Application

1.  **Start the frontend development server:**
    ```bash
    cd frontend
    npm start
    ```
    The frontend will be available at `http://localhost:3000`.

2.  **Start the backend server:**
    ```bash
    cd ../backend
    python server.py
    ```
    The backend will be available at `http://localhost:5000`.

## 📂 Project Structure

```
.
├── backend/
│   ├── server.py
│   └── requirements.txt
├── frontend/
│   ├── public/
│   └── src/
│       ├── components/
│       ├── pages/
│       └── ...
└── README.md
```
